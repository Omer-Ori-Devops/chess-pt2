apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-backend
  labels:
    app: chess
spec:
  selector:
    instance: {{ .Release.Name }}-pod-backend
  ports:
    - port: 5000
      targetPort: 5000
